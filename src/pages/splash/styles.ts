import { View } from "react-native";
import styled from "styled-components";

export const SplashBg = styled(View)`
    flex:1;
    align-items: center;
    justify-content: center;
    background-color: #1D1C3E;
`